# 🤖 Сигмоида (Sigma) - Ваш Персональный AI Ассистент в Telegram

![Статус проекта: Бета](https://img.shields.io/badge/Статус%20проекта-Бета-yellow.svg)

## 🚀 О проекте

Привет! Я — Сигмоида, ваш продвинутый AI ассистент, который живёт в Telegram. Моя миссия — сделать взаимодействие с нейросетями простым, увлекательным и доступным прямо из вашего чата. Забудьте о сложностях — я здесь, чтобы помочь вам творить, общаться и даже создавать свои мини-игры без лишних усилий.

Сигмоида — это пет-проект, созданный с энтузиазмом и без огромного бюджета. Важно понимать, что все данные, которыми вы делитесь со мной, обрабатываются через API сторонних провайдеров (которых я называю «челиками» или «провайдерами»). Эти данные могут быть использованы для обучения их последующих моделей. Я ценю вашу конфиденциальность и предоставляю инструменты для её управления.

Изначально Сигмоида был простым ботом, способным отвечать на сообщения и корректироваться через ответы. Но теперь это настоящий гигабот, который умеет гораздо больше! Я не только веду диалоги, но и могу генерировать изображения, создавать полноценные мини-игры и многое другое.

## ✨ Возможности Сигмоиды

### 💬 Умные Диалоги и Управление Контекстом

*   **Долго болтаем — контекст не теряю:** Я умею сжимать историю чата, чтобы поддерживать контекст беседы. Сначала использую DeepSeek через OpenRouter, а если что-то идёт не так, переключаюсь на ближайший доступный ключ Gemini.
*   **Выбор провайдера:** Управляйте тем, какая нейросеть будет отвечать вам. Используйте команду `/set_provider gemini|openrouter|pollinations|auto` для выбора между доступными провайдерами или позвольте мне автоматически переключаться.
*   **Ротация ключей:** Я использую несколько API ключей для каждого провайдера и автоматически переключаюсь между ними, чтобы обеспечить стабильную работу.
*   **Системный промпт:** У меня есть "систем промпт" — это то, как я формирую свою личность.

### 🎨 Генерация Изображений

*   **Рисую по команде:** Просто отправьте `/draw <описание>`, и я создам изображение по вашему запросу.
*   **Выбор модели для картинок:** Через `/set_draw_model` вы можете выбрать предпочитаемую модель Pollinations (например, `flux`, `flux-realistic`, `flux-anime` и т.п.).

### 🎮 Создание Игр на Phaser

*   **Делаю мини-игры на Phaser по твоей идее:** Используйте команду `/game <идея>`, и я сгенерирую полноценную игру на Phaser, которую можно запустить прямо в веб-песочнице.
*   **Удобный шеринг:** Сгенерированные игры удобно шарить через веб-хаб.

### 👥 Управление в Группах

*   **Живу в группах без админки:** Я могу полноценно работать в групповых чатах, не требуя прав администратора.
*   **Автопостинг:** Настройте автоматические посты в чате с помощью команд `/autopost on|off`, `/set_interval <сек>` и `/set_minmsgs <n>`.
*   **Размер ответа:** Управляйте длиной моих ответов с помощью `/set_msgsize <s|m|l>` (small, medium, large или пусто для default).

### 🌐 Веб-Интеграция

*   **Веб-логин:** Получите персональный код для входа на сайт с играми и песочницей с помощью команды `/login`.
*   **Веб-хаб:** На сайте вы сможете просматривать и играть в сгенерированные игры, а также делиться ими.

### 🔒 Конфиденциальность и Данные

*   **Забочусь о данных:** История ваших чатов и настройки хранятся в Redis.
*   **HTML форматирование:** Я форматирую ответы красивыми HTML тегами Telegram для лучшей читаемости.
*   **Контроль данных:** Используйте `/privacy` для ознакомления с политикой конфиденциальности и `/delete_data chat_id` для удаления ваших данных из чата.

## 🧠 Провайдеры и Модели

Я поддерживаю интеграцию с несколькими мощными LLM провайдерами, каждый из которых имеет свои особенности:

### Gemini

*   **Доступные модели:** В основном `gemini-2.5-pro` (самая мощная из доступных, занимает 1 место в рейтинге lmarema.ai).
*   **Ограничения:** `gemini-2.5-pro` можно использовать до 100 раз в день (при отсутствии других пользователей). После этого автоматически переключится на модель, находящуюся на 18 месте.
*   **Использование:** Приоритетный провайдер по умолчанию для самых сложных задач.

### OpenRouter

*   **Лучшие бесплатные модели:** DeepSeek (`deepseek-r1-0528` — лучший, затем `deepseek-r1t2-chimera` и чатовский DeepSeek).
*   **Выбор модели:** Вы можете выбрать конкретную модель OpenRouter с помощью команды `/set_or_model <название>`.
*   **Ограничения:** Суммарно 200 запросов в день на все модели OpenRouter.

### Pollinations

*   **Модели для изображений:** `flux`, `gptimage`, `turbo`. Я сам знаю только `flux`, но, если `gptimage` — это `gpt-image-1`, то он может занимать от 6 до 9 места в рейтинге, что делает его самым крутым для изображений в Сигмоиде.
*   **Текстовые модели:** Здесь представлены модели, которые могут быть менее мощными по сравнению с Gemini и OpenRouter, но имеют свои преимущества.
    *   `gemini-flash-lite` с поисковиком от Google (без поисковика занимает 50 место в рейтинге) — единственная модель с доступом в интернет.
    *   `o4-mini` (40 место в рейтинге).
*   **Выбор модели:** Вы можете выбрать конкретную текстовую модель Pollinations с помощью команды `/set_pollinations_text_model <название>`.

## 💡 Мини-подсказки для старта

*   **Очистить диалог:** Хотите, чтобы я "откатила" и начала чистый диалог? Используйте команду `/reset`.
*   **Сменить текстовую модель Pollinations:** `/set_pollinations_text_model <название>`.

## 📋 Все команды бота

* `/start` — начать работу с ботом
* `/help` — показать список всех команд
* `/settings` — показать текущие настройки чата
* `/privacy` — политика конфиденциальности
* `/reset` — очистить историю диалога
* `/draw <описание>` — нарисовать изображение по описанию
* `/game <идея>` — сгенерировать игру на Phaser по идее
* `/login` — получить код для входа на веб-сайт
* `/autopost on|off` — включить/выключить автопосты (только для админов)
* `/set_interval <сек>` — установить интервал автопоста (только для админов)
* `/set_minmsgs <n>` — установить минимум сообщений для автопоста (только для админов)
* `/set_msgsize <s|m|l>` — установить размер ответа (только для админов)
* `/set_draw_model <название>` — выбрать модель для генерации изображений
* `/set_pollinations_text_model <название>` — выбрать текстовую модель Pollinations
* `/set_or_model <название>` — выбрать модель OpenRouter
* `/set_provider <gemini|openrouter|pollinations|auto>` — выбрать провайдера LLM
* `/delete_data [chat_id]` — удалить данные чата (для админов с указанием chat_id, для пользователей — без параметров)

## 🏗️ Архитектура проекта

Сигмоида построена на модульной архитектуре, состоящей из нескольких ключевых компонентов:

### Основные компоненты

1. **Telegram Bot (`app/bot/`)** — обработка сообщений и команд от пользователей
   - `handlers.py` — обработчики команд и сообщений
   - `jobs.py` — фоновые задачи (автопостинг, проверка моделей)

2. **LLM Client (`app/llm/`)** — интеграция с провайдерами языковых моделей
   - `client.py` — основной клиент для работы с Gemini, OpenRouter и Pollinations
   - Поддержка ротации ключей и автоматического переключения между провайдерами
   - Сжатие истории чата для сохранения контекста

3. **Game Generator (`app/game/`)** — генерация игр на Phaser
   - `generator.py` — создание игр на основе текстовых описаний
   - Валидация JavaScript-кода перед сохранением
   - Поддержка ревизий и доработок игр

4. **Web Server (`app/web/`)** — веб-интерфейс для игр
   - `server.py` — Flask-приложение с API для игр и аутентификации
   - Веб-хаб для просмотра и игры в сгенерированные игры

5. **Storage (`app/storage/`)** — хранение данных
   - `redis_store.py` — работа с Redis для хранения истории, настроек и игр

6. **Security (`app/security/`)** — управление конфиденциальностью
   - `privacy.py` — политика конфиденциальности и управление данными

## 🛠️ Установка и настройка

### Требования

* Python 3.10+
* Redis (локально или облачный, например Upstash)
* API ключи от провайдеров (Gemini, OpenRouter, Pollinations)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd сигмоида
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` с необходимыми переменными окружения (см. раздел "Переменные окружения")

4. Запустите бота:
```bash
python app/main.py
```

### Структура проекта

```
сигмоида/
├── app/
│   ├── bot/              # Telegram бот
│   │   ├── handlers.py   # Обработчики команд
│   │   └── jobs.py       # Фоновые задачи
│   ├── game/             # Генерация игр
│   │   └── generator.py
│   ├── llm/              # LLM клиенты
│   │   └── client.py
│   ├── security/         # Безопасность
│   │   └── privacy.py
│   ├── storage/          # Хранилище
│   │   └── redis_store.py
│   ├── utils/            # Утилиты
│   │   └── text.py
│   ├── web/              # Веб-сервер
│   │   └── server.py
│   ├── config.py         # Конфигурация
│   ├── main.py           # Точка входа
│   └── state.py          # Состояние приложения
├── webapp/               # Веб-интерфейс
│   ├── hub.html          # Главная страница
│   ├── sandbox.html      # Песочница для игр
│   ├── privacy.html      # Политика конфиденциальности
│   └── ...
├── requirements.txt      # Зависимости
└── README.md            # Документация
```

## ⚙️ Переменные окружения

### Обязательные переменные

```env
# Telegram
TG_TOKEN=your_telegram_bot_token
ADMIN_ID=your_admin_telegram_id

# Redis
REDIS_URL=redis://localhost:6379
# Или для Upstash:
# REDIS_URL=rediss://default:password@host.upstash.io:6379

# Gemini API (минимум один ключ)
GEMINI_API_KEY_1=your_gemini_api_key_1
GEMINI_API_KEY_2=your_gemini_api_key_2  # Опционально
```

### Опциональные переменные

```env
# OpenRouter
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_API_KEY_1=your_openrouter_api_key_1  # Дополнительные ключи
OPENROUTER_MODELS=deepseek/deepseek-chat-v3-0324:free,deepseek/deepseek-r1-0528:free
OPENROUTER_SITE_URL=https://your-site.com
OPENROUTER_SITE_NAME=Your Site Name
OPENROUTER_TIMEOUT=45

# Pollinations
POLLINATIONS_ENABLED=true
POLLINATIONS_API_KEY=your_pollinations_api_key
POLLINATIONS_TEXT_BASE_URL=https://text.pollinations.ai/openai
POLLINATIONS_TEXT_MODELS=o4-mini,gemini-search
POLLINATIONS_TEXT_DEFAULT=o4-mini
POLLINATIONS_TEXT_TEMPERATURE=1.0
POLLINATIONS_TEXT_TIMEOUT=120.0
POLLINATIONS_MODEL=flux
POLLINATIONS_MODELS=flux,flux-realistic,flux-anime
POLLINATIONS_BASE_URL=https://image.pollinations.ai
POLLINATIONS_WIDTH=1024
POLLINATIONS_HEIGHT=1024
POLLINATIONS_SAFE_MODE=true
POLLINATIONS_PRIVATE_IMAGES=true
POLLINATIONS_NO_LOGO=true

# Настройки провайдеров
LLM_PROVIDER_ORDER=gemini,openrouter,pollinations
IMAGE_MODEL_NAME=gemini-2.0-flash-preview-image

# Веб-приложение
WEBAPP_BASE_URL=https://your-site.com
FLASK_HOST=0.0.0.0
FLASK_PORT=10000
FLASK_SECRET_KEY=your_secret_key
SESSION_COOKIE_NAME=sig_session

# Игры
GAME_TTL_SECONDS=604800  # 7 дней

# Безопасность
DOWNLOAD_KEY=your_download_key  # Для скачивания истории через веб
LOGIN_CODE_TTL_SECONDS=600  # 10 минут
```

## 🔧 Технологический стек

* **Python 3.10+** — основной язык программирования
* **python-telegram-bot** — библиотека для работы с Telegram Bot API
* **google-genai** — SDK для работы с Google Gemini API
* **Flask** — веб-фреймворк для веб-интерфейса
* **Redis** — хранилище данных (история, настройки, игры)
* **Phaser 3** — фреймворк для создания игр в браузере
* **requests** — HTTP-клиент для работы с внешними API

## 🔄 Как это работает

1. **Получение сообщения:** Бот получает сообщение от пользователя в Telegram
2. **Обработка:** Сообщение обрабатывается соответствующим обработчиком
3. **Выбор провайдера:** Система выбирает подходящий LLM провайдер (Gemini, OpenRouter или Pollinations)
4. **Генерация ответа:** Запрос отправляется к выбранному провайдеру с учетом истории чата и системного промпта
5. **Сжатие контекста:** Если история чата слишком длинная, она сжимается через summarization
6. **Сохранение:** История диалога и настройки сохраняются в Redis
7. **Отправка ответа:** Ответ отправляется пользователю с форматированием HTML

### Генерация игр

1. Пользователь отправляет команду `/game <идея>`
2. Система формирует промпт для генерации игры на Phaser
3. LLM генерирует JavaScript-код игры в формате JSON
4. Код валидируется на синтаксические ошибки
5. Игра сохраняется в Redis с уникальным ID
6. Пользователю отправляется ссылка на игру в веб-песочнице

## 📝 Лицензия

Copyright (c) 2025 sprouee

---

Это всё, что я умею на данный момент. Я постоянно учусь и развиваюсь, чтобы быть ещё полезнее!