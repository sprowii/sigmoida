# Cигмоида — Telegram-бот на базе Google Gemini

Сигмоида — многофункциональный ассистент для Telegram, использующий модели семейства Google Gemini. Бот ведет диалог, умеет работать с изображениями, аудио и видео, поддерживает автопосты и сохранение настроек.

Важно: бот форматирует ответы в Telegram с помощью HTML-тегов (<b>, <i>, <code>, <pre>, <a> и др.).

## 🔥 Ключевые возможности

- **🧠 Диалоговый интеллект**: ответы на основе актуальных моделей Google Gemini.
- **🎨 Генерация изображений**: команда `/draw <описание>` или по контексту через инструменты.
- **🎤 Медиа-понимание**: расшифровка голосовых, описание видео и «кружочков».
- **💾 Долгосрочная память**: автоматическая суммаризация длинных диалогов.
- **🛠️ Инструменты (Function Calling)**: модель сама решает, когда вызвать генерацию изображения.
- **⚙️ Настройки**: автопосты, размер ответов, интервалы и т.д.
- **🔒 Сохранение данных**: история и конфиги пишутся в JSON, переживают перезапуски.
- **🚀 Простое развертывание**: готов для бесплатного тарифа Render.

## 🔧 Установка и запуск

### 1) Требования

- Python 3.9+
- Аккаунт в Google AI Studio (API-ключи Gemini)
- Telegram-токен от @BotFather

### 2) Настройка окружения

1. Клонировать репозиторий:
   ```bash
   git clone <адрес_репозитория>
   cd <папка_репозитория>
   ```

2. Виртуальное окружение и зависимости:
   ```bash
   python -m venv venv
   # Windows
   venv\Scripts\activate
   # Linux/Mac
   # source venv/bin/activate
   pip install -r requirements.txt
   ```

3. Переменные окружения (пример .env):
   ```env
   TG_TOKEN="123456:ABC..."
   ADMIN_ID="123456789"
   GEMINI_API_KEY_1="AIzaSy...KEY1"
   GEMINI_API_KEY_2="AIzaSy...KEY2"
   PORT="10000"
   ```

### 3) Запуск локально

```bash
python сигмоида.py
```

## 🚀 Деплой на Render

Проект содержит `render.yaml` для автоконфигурации.

1. Создайте сервис Blueprint и укажите репозиторий.
2. Добавьте переменные окружения (`TG_TOKEN`, `ADMIN_ID`, `GEMINI_API_KEY_*`, `PORT`).
3. Дождитесь деплоя.

## 🤖 Команды

- `/start` — приветствие
- `/help` — список команд
- `/draw <описание>` — сгенерировать изображение
- `/reset` — очистить историю
- `/privacy` — политика конфиденциальности

### 👑 Для администратора

- `/settings` — показать текущие настройки
- `/delete_data <chat_id>` — удалить данные чата
- `/autopost <on|off>` — включить/выключить автопосты
- `/set_interval <сек>` — интервал автопостов
- `/set_minmsgs <n>` — минимум сообщений для автопоста
- `/set_msgsize <small|medium|large>` — формат ответов

---
Удачи и продуктивных разговоров! 💬